
Процедура ПриЗаписи(Отказ)
	НоваяЗапись = РегистрыСведений.СрокЧитательскогоБилета.СоздатьМенеджерЗаписи(); 
	НоваяЗапись.Период = ТекущаяДата();
	НоваяЗапись.ЧитательскийБилет = Ссылка;
	НоваяЗапись.СрокДействия = ДобавитьМесяц(ТекущаяДата(), 12); // Будем выдавать на год
	НоваяЗапись.Записать(Истина);
КонецПроцедуры
